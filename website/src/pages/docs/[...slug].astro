---
import DocArticlePage from "../../templates/DocArticlePage.astro";
import { docsStaticPaths } from "../../lib/docs";

export function getStaticPaths() {
  return docsStaticPaths("zh");
}

const { entry, locale, docs } = Astro.props;
---

<DocArticlePage locale={locale} entry={entry} docs={docs} />
