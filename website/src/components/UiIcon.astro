---
import { Icon } from "astro-icon/components";

interface Props {
  name: string;
  class?: string;
}

const { name, class: className = "size-4" } = Astro.props;
---

<Icon name={name} class={className} aria-hidden="true" />
